# Pipeline News release naming

New release folders, release IDs and manifest filenames use `YYYYMMDDHHmm-pipelinenews`.

`pipelinenews` is the permanent lowercase path slug. The visible product title remains `Pipeline News`. Historical uppercase release paths remain immutable and are not renamed.
