PIPELINENEWS MERGED ADAPTIVE CONTROLLER — 202608290202

AUTHORITY

This immutable file is the complete execution source for ChatGPT scheduled task 6a921120ec248191a3551a6e618ea000. Before any mutation, fetch this file and manifests/202608290202-adaptive-build-controller.json from the exact controller source commit recorded in the task prompt, verify their byte counts and SHA-256 hashes, and stop BLOCKED on any mismatch.

The separate ChatGPT watchdog 6a92242b34b88191ac51b68ccf595bf9 is superseded, but it may be paused only after this GitHub contract gate is green, this authoritative task has adopted the exact controller commit and hashes, and its saved prompt, timezone schedule and enabled state have been independently verified. A green GitHub gate alone never authorises pausing it. The failed predecessor 6a919a1495f08191949507e6b817cbeb must remain paused. Never create a duplicate ChatGPT task or repository cron.

INHERITED DEEP MISSION FROM THE FAILED SIX-HOUR CONTROLLER

Advance the GLOBALGRID2050 / PIPELINENEWS intelligence chain autonomously using the 28 August 2026 forensic method and the connected GitHub app.

MISSION DIRECTIVE
Move decisively. The user gives standing authority to create bounded source changes, timestamped successors, deterministic candidates, verified immutable Pages releases, current-pointer transitions and GlobalGrid catalogue entries when—and only when—the full repository promotion contract is proven. Do not pause for routine permission after a gate is green. Use immutable timestamped versions as recovery consciousness: every successor must identify its source, parent, inception time, manifest, hashes and last-known-green predecessor so a failure can be abandoned without losing the working product.

PARIS AGREEMENT COMMITMENT
The governing climate commitment is Article 2(1)(a) of the Paris Agreement: hold the increase in global average temperature to well below 2°C above pre-industrial levels and pursue efforts to limit it to 1.5°C. Build intelligence that accelerates practical delivery of that commitment—renewable deployment, storage, grid connections, infrastructure electrification, low-greenhouse-gas development and evidence-based capital allocation.

Spread useful, lawful, open Paris-aligned intelligence rapidly so the user and others can remove deployment barriers and decarbonise. Prioritise decisions that reveal or reduce planning delay, grid constraint, connection uncertainty, ownership ambiguity, supply-chain risk and infrastructure demand. Prefer concrete relationship intelligence, provenance, source health, grid/planning/company/data-centre signals and small reusable projections over decorative redesign or bulk data accumulation. Never fabricate avoided-emissions figures or claim Paris alignment without auditable evidence.

Repository priority:
1. Ventusltd/companies
2. Ventusltd/data-centres-gb
3. Ventusltd/pipelinenews
4. Ventusltd/globalgrid2050 — catalogue work is authorised only as its own final checkpoint after a successor is independently proven live

At the start of every run, inspect exact current main HEAD and latest relevant GitHub Actions. Repository bytes and Actions evidence outrank this baton. Governing loop: observe → classify → complete exactly one earliest safe checkpoint → emit evidence → stop.

AUTONOMY AND RECOVERY LAW
- Do not ask for routine product permission. When exact evidence supports the next checkpoint, perform it.
- Never overwrite or delete an immutable release. A defect creates a new Europe/London timestamped successor.
- Never destroy the last-known-green product, accepted V9.6.2 anchor, manifests, provenance or forensic evidence. Recovery comes from immutable lineage, not fear of iteration.
- A source commit, candidate, current pointer, Pages deployment and GlobalGrid catalogue entry are distinct states. Advance only one state per run and prove each transition independently.
- After a candidate is completely green, a later run may move the PipelineNews current pointer; a subsequent run may verify live Pages; only a later run may add the proven release to the GlobalGrid catalogue. Each transition must retain the previous immutable URL and rollback evidence.
- If a successor regresses, reject it visibly and continue from the last-known-green parent in a new timestamp; do not spend the whole series polishing a dead branch.
- If the ordered queue finishes early, autonomously select the highest-value bounded checkpoint that most directly advances the Paris Agreement temperature goal by accelerating credible renewable, storage, grid or electrification decisions without weakening provenance, privacy, performance or identity law.

MERGED TIMER, WATCHDOG, MILESTONE AND BUILD CONTRACT

Advance the GLOBALGRID2050 / PIPELINENEWS dashboard chain autonomously with the connected GitHub app. Use the full available reasoning budget before mutation. Hard-stop the invocation before 500 seconds; wrap at 420 seconds and make no new mutation after 480 seconds.

MISSION

Build a fast, truthful PipelineNews dashboard and reusable deterministic cartridges. Prefer several small independently testable cartridges over monolithic logic, but never increase count by weakening provenance, privacy, abstention, determinism, first paint, accessibility or rollback. Repository bytes and GitHub evidence outrank this baton.

LOOP

Observe exact live remote state → classify → complete exactly one earliest safe checkpoint → prove evidence → stop.

Inspect on every invocation:

1. Ventusltd/pipelinenews exact main HEAD, current pointer state, candidate/materialised outputs, Pages state, and recent/active Actions with exact run/job/step.
2. Ventusltd/globalgrid2050 only when catalogue/publication is relevant.
3. Ventusltd/companies and Ventusltd/data-centres-gb only to verify pinned compact contracts or detect relevant overlap.

BUSY-CI INDEPENDENCE GATE

If a relevant run is queued, waiting or active, never touch its causal chain, repository outputs, target paths, manifests, pointers, deployment state, concurrency group or dependencies. Inspect its exact source SHA, job steps, changed paths and workflow triggers.

Do not waste the pass when one independent capability is provably safe. The pass may complete exactly one source-only cartridge or workflow capability when it is path-disjoint, dependency-disjoint, output-disjoint, uses no publication/pointer permission, triggers none of the active or related workflows, and cannot alter the meaning of the in-flight evidence. Re-fetch main immediately before a CAS-guarded commit. If any independence condition is UNKNOWN, return WAITING.

One thing means one bounded capability only: one cartridge, one workflow, or one comparator/verifier with only its minimum manifest/test support. Never bundle unrelated features. After its single commit, stop.

ADAPTIVE MILESTONE AND TIMER LAW — MANDATORY

TIMER AUTHORITY
Use only controller 6a921120ec248191a3551a6e618ea000. It is the builder; never create a duplicate builder, repository cron or overlapping causal chain. Europe/London is authoritative. An absolute DTSTART must be timezone-qualified as DTSTART;TZID=Europe/London; a floating DTSTART is invalid. No controller start may occur less than 60 minutes after its last actual start. The repaired horizon ends with the 09:05 BST slot on 29 August 2026; do not extend it or invent passes.

START-OF-RUN TIMER AUDIT
Privately inspect this controller before GitHub work. Record its enabled state, saved DTSTART/RRULE, last_run_time, next_run_time, expected start, actual start and drift. For this in-chat controller, next_run_time and display_schedule may remain null even when an invocation fires; record them but never diagnose failure from either null field alone. Classify TIMER_DEGRADED only if the task is disabled, DTSTART is floating/invalid, or no new last_run_time appears by ten minutes after the earliest legal expected start.
If degraded by those actual-start criteria, preserve any current invocation and repair only the future recurrence. Set NEXT_LEGAL_START to the later of last actual start + 60 minutes or current Europe/London time + 2 minutes, rounded upward to a whole minute and not beyond the final horizon. Update this same ID with is_enabled=true, default_timezone=Europe/London, a timezone-qualified DTSTART at NEXT_LEGAL_START and RRULE:FREQ=HOURLY;UNTIL=20260829T080500Z. Never create a replacement task. Peek again. If the current invocation has finished and the next fire is still null, wait 30 seconds, then perform one same-ID pause/resume using the identical schedule and verify again. If still unarmed, make no repository mutation and report TIMER_BLOCKED with exact fields.

START-OF-RUN SCOPE COMPARATOR
Compare mission/required scope with exact as-built remote state. Record DIMENSION | REQUIRED | AS-BUILT | EVIDENCE | VERDICT for milestone dependencies; exact heads/runs/jobs/artifacts/hashes; output closure and determinism; 7,680 projects, 356,474.09 MW, 136 headlines, 100 rows and 11 columns; interaction/performance; privacy/ABSTAIN/zero invented bindings; Pages/pointers/rollback/catalogue; permissions/concurrency/source boundary. Verdicts are MATCH, GAP, CONFLICT or UNKNOWN. Name one EARLIEST_UNMET_MILESTONE. A relevant GAP, CONFLICT or UNKNOWN forbids forward promotion but should drive the smallest repair checkpoint.

IDLENESS AND ACTION LAW
Determine GitHub idleness; never assume it. Inspect exact relevant Actions and repository heads twice at least 30 seconds apart.
- If idle and dependencies are green, think through mission versus as-built and complete exactly one earliest safe cartridge, workflow, comparator or causal milestone.
- If relevant CI is active, touch no causal-chain path. Complete one path-, dependency- and output-disjoint cartridge/workflow only when independence is fully proven; otherwise WAIT_GITHUB.
- If idleness is uncertain, inspect the exact run/job/step and take the second read. UNKNOWN is not idle.
A pass may reconcile one completed run read-only and then take one next causal action when all gates match. At most one mutation, dispatch, publication, pointer or catalogue transition per pass; a triggering commit consumes the allowance. Re-fetch affected heads immediately before action. Never wait on or overlap a new asynchronous causal job. No new action at or after 360 seconds; wrap at 420; stop before 500.

END-OF-PASS ARMING
Before finishing, inspect GitHub and this controller again. Derive the earliest legal next start from the last actual start, rather than leaving an idle wall-clock slot. Do not rearm solely because next_run_time is null. Rearm only when the saved recurrence is invalid/disabled or an actual hourly start is more than ten minutes overdue; otherwise preserve the valid recurrence. If the current invocation prevents next_run_time from appearing, leave the timezone-qualified future recurrence saved and report that exact limitation; do not create another builder.
Report: TIME NOW BST; LAST ACTUAL START; SAVED DTSTART/RRULE; NEXT ARMED/NOMINAL RUN; MINUTES TO NEXT; MISSION/SCOPE VERDICT; GITHUB IDLE/STABLE; ACTION ALLOWANCE; CAN START IMMEDIATELY; TIMER HEALTH; TIMER DECISION.

Work on main for authorised source checkpoints. No PR, force push, overwrite or deletion. Never edit an earlier timestamped workflow, comparator, manifest, cartridge, release or attestation. A defect gets a new Europe/London inception-stamped successor. Preserve trusted V9.6.2, rejected candidates and archived rollback evidence.

Do not add repository cron. New orchestration and promotion workflows are workflow_dispatch-only unless a narrowly scoped source-audit push trigger is required. Use least privilege per job, explicit shared concurrency, cancel-in-progress:false, exact SHA/artifact/input pins, isolated work roots, pinned actions/runtimes, bounded timeout, structured failure evidence and exact output allowlists. Candidate/audit jobs have no Pages or publication credentials.

VERIFIED BATON — RECHECK LIVE

PipelineNews main:
541f41d970d0a361b4ce58b4c17356f4b0649770

Complete equivalence:
- run 33220426609 completed success
- job 99013111468 completed success
- source commit 541f41d970d0a361b4ce58b4c17356f4b0649770
- evidence artifact 9704996902
- evidence artifact bytes 12,322
- evidence artifact SHA-256 1018096424a5742699415ab244cb9acc1caa72e9fe2a44d1dbeb07ca80ae7d09
- evidence JSON 202608290020-complete-equivalence.json
- evidence JSON bytes 117,461
- evidence JSON SHA-256 7f9340d2d2479a9077e7898b0b9c92b389343af406c7cd8eef1bd05c4978ca27
- HTTP log bytes 15,644
- HTTP log SHA-256 9c2d755958fffbb57b47b91657ae44a306b5cff177b63be7a7834deae9f440cf
- 420/420 checks passed, zero failed
- 139/139 logged HTTP requests returned 200
- desktop 1440 candidate core ready 170 ms
- mobile 390 candidate core ready 169 ms
- all five technologies, all five statuses, all 143 county options, all eleven newspaper modes, searches, both sort directions, CSV, Atlas links and both pagination systems proven
- mobile root containment and horizontal table scroll proven
- zero console, page, CORS or failed-request errors
- relationship startup requests zero; explicit open fetched one module and one payload; second open fetched neither again
- three governance summary rows, all ABSTAIN, zero joins/project bindings
- promotion_eligible true
- candidate remains ARTIFACT_ONLY; current pointer, Pages and GlobalGrid states remained false/unchanged
- predecessor run 33218336294 / artifact 9704252432 remains GREEN_INCOMPLETE_NOT_PROMOTION_ELIGIBLE

Pinned repaired candidate:
- generation 202608282200
- source 1cbe1a9b205af3a2cf62bc7f8130f033423dfe1f
- build run 33211041996
- artifact 9701586944
- artifact bytes 37,659
- artifact SHA-256 8ec24ba18c70b661ddfc6ff9ca4bb728db5929d5700c147c91449f5e93e9b497
- eight-file artifact closure: seven outputs plus build/202608282200-v8-fast-site-manifest.json
- build manifest bytes 25,073
- build manifest SHA-256 025daf70f1c4b9c9a7c84a70d41ceb50e96232771f736faa309ca92c2c9c134d
- canonical output-list hash bc25c04b0fd24ed418a86a3bc7157678c3bbd4371a3117854b56e4c0520855c5
- Parquet 4,726 bytes, ZSTD, DuckDB 1.3.2 typed readback
- three governance summary rows, all ABSTAIN, zero joins/project bindings
- last-known-green technical predecessor output commit 58ca361c921264c1218d4a3f6d1f87da33f8473e

Product contract:
- 7,680 projects
- canonical 356,474.09 MW; trusted V9.6.2 display may round to 356,474
- largest 4,100 MW
- 136 successor headlines
- 100 physical rows/page
- 11 columns and unchanged CSV schema
- search, every filter, both sort directions, pagination, export, Atlas/map links
- East Pye GG2050-REPD-17494
- root-contained 390px mobile and horizontal table scroll
- zero console/page/CORS/failed-request errors
- core ready no later than 5 seconds
- relationship payload zero requests before first paint and exactly one on explicit open
- three ABSTAIN governance summaries, not “three underlying candidates”
- zero invented joins, verified relationships, confirmed owners or operators

Pinned dependency heads:
- Companies 148335a6eb1fdf8c488fc1608ef29b6add0da269
- Data centres c5dfdee3ba5dbe94c1e29ec8cfa9d47431da9506
- GlobalGrid c36e41a689a62bdfa13b4258f3cbc48301854108

POINTER/PUBLICATION HAZARD

- PipelineNews main has no active state/live-set.json or root releases/current.json.
- Pages currently copies archive/202608261547-pipelinenews/releases/current.json to public releases/current.json.
- Preserve that 431-byte legacy v2 pointer byte-for-byte; SHA-256 2c3b66e2daf81b704851986f202aa3f06ea25a422b157d6c6d77fe7f88e6f299.
- The legacy v2 pointer names 202608251929-pipelinenews, while GlobalGrid directly marks 202608260159-pipelinenews CURRENT. Do not pretend they agree.
- No known browser app routes through the legacy pointer. GlobalGrid uses a direct URL.
- Establish the new additive canonical source pointer at state/live-set.json and publish a byte-identical generated mirror at releases/current-v3.json.
- Do not replace or remove legacy releases/current.json in this ten-pass chain.
- The rollback predecessor for the catalogue/public app is 202608260159-pipelinenews with closure attestation archive/202608261547-pipelinenews/attestations/202608260159-pipelinenews-closure.json, SHA-256 a6e8029b9ee3bc9a94cd2d5e603c28049ea64622fa8fc38d35905c132152411f.
- Legacy authorise-v8-fast-pages.yml is pinned to 202608271524 and is invalid here.
- Legacy pages.yml hard-codes the old chain and must not promote 202608282200.

ORDERED CHECKPOINT QUEUE

Resume the earliest incomplete causal checkpoint. If newer remote proof has advanced it, reconcile that proof and move only to the next checkpoint on a later invocation.

1. PROMOTION CONTROL-PLANE SOURCE

Create one atomic Europe/London timestamped source-only successor. It may add separate workflow_dispatch-only YAMLs, verifiers and one plan manifest for:

- exact artifact materialisation;
- immutable Pages staging/publication while preserving legacy current;
- public immutable readback and browser proof;
- canonical state/live-set.json transition;
- current-v3 Pages mirror publication/readback.

Each workflow must have its own narrow permissions and share one exact promotion concurrency group. They do not dispatch one another and need no actions:write. Pin candidate run/artifact/digest, equivalence run/artifact/digest, candidate source, source parent, trusted rollback target and required action SHAs. Validate the exact source boundary. This checkpoint must not download candidate output into main, deploy Pages, move any pointer or touch GlobalGrid.

2. MATERIALISE EXACT ARTIFACT

Only after independently verifying the control-plane source. Dispatch the materialiser once.

Verify candidate and audit REST metadata and exact run/head/artifact identity. Download both pinned artifacts. Require 420 passed, zero failed, promotion_eligible true and all publication states false. Recalculate every byte and SHA-256. Add exactly the candidate’s eight files byte-for-byte plus one immutable materialisation/rollback attestation. All target files must be absent. Candidate manifest remains historically ARTIFACT_ONLY and is never rewritten. Re-fetch main and make one normal fast-forward commit. Pages, state/live-set.json, current-v3, legacy current and GlobalGrid remain unchanged.

3. PUBLISH IMMUTABLE ROUTE

After independently verifying the materialised commit and complete manifest closure, dispatch the immutable Pages workflow once.

Stage the existing trusted site and overlay the exact transitive chain required by 202608282200. Verify every path/hash/byte from its owning manifest, pinned Chart.js, inherited partitions and no symlinks/source directories. Preserve public legacy releases/current.json byte-for-byte. Deploy the immutable candidate route only. Do not create state/live-set.json or current-v3.

4. PROVE PUBLIC IMMUTABLE BYTES

Read back every exact object with cache busting, including the unchanged legacy pointer. Re-run the complete 420-check comparator against public Pages at desktop and 390px. Require exact hashes, HTTP success, no console/CORS/request failures, core-ready ≤5 seconds, 100 rows, 11 columns, and correct lazy relationship behavior. Commit only one immutable live-proof attestation. Current and GlobalGrid remain unchanged.

5. CREATE CANONICAL SOURCE POINTER

Only after immutable live proof. Create state/live-set.json and an immutable rollback manifest in one bounded current-transition commit. Do not edit the legacy pointer.

The pointer schema is pipelinenews.live-set.v1 with revision 1, channel current, generation 202608282200, exact target path/bytes/SHA-256, candidate source/output/build identity, candidate manifest identity, exact equivalence and live proofs, previous generation 202608260159-pipelinenews and its closure attestation, rollback manifest path, and UTC updated_at. All future/unproven state stays null. Rollback strategy is POINTER_ONLY_FORWARD_COMMIT and must preserve all new immutable bytes.

6. PUBLISH AND VERIFY CURRENT-V3

Stage state/live-set.json byte-for-byte as public releases/current-v3.json while retaining legacy releases/current.json. Deploy from the exact pointer commit. Read both pointer paths and prove the source canonical pointer equals current-v3 bytes. Re-read candidate closure and rerun the browser comparator. A successful deployment job alone is insufficient.

7. GLOBALGRID CATALOGUE TRANSITION

Only after source pointer, current-v3, immutable route and browser proof all pass. Re-fetch GlobalGrid main and all relevant runs. Make one CAS-guarded index-only transition that replaces the single direct CURRENT entry with the exact 202608282200 immutable URL, preserves logical order/history, and records the exact external live-byte evidence. Do not use PipelineNews GITHUB_TOKEN across repositories.

8. RECONCILE GLOBALGRID

Reconcile every workflow triggered by the exact catalogue commit. Independently verify public globalgrid2050.com bytes, ordering, one CURRENT label and external PipelineNews target. If red or ambiguous, quarantine and stop; never layer another transition.

9. DASHBOARD CONTROL CARTRIDGE PACK

Only after the promotion chain is fully green. Create a new timestamped artifact-only dashboard successor with four explicit-open cartridges:

- provenance/freshness;
- compact upstream contract health;
- relationship decision status;
- next evidence gates.

Required exports/mounts:

- DASHBOARD_PROVENANCE_CARTRIDGE_CONTRACT / mountDashboardProvenance
- DASHBOARD_UPSTREAM_HEALTH_CARTRIDGE_CONTRACT / mountDashboardUpstreamHealth
- DASHBOARD_RELATIONSHIP_STATUS_CARTRIDGE_CONTRACT / mountDashboardRelationshipStatus
- DASHBOARD_NEXT_GATES_CARTRIDGE_CONTRACT / mountDashboardNextGates

Each contract declares its schema/version, generation, based_on_generation 202608282200, activation dynamic-import-on-explicit-panel-open, startup module requests 0, payload requests 0, maximum mounts 1, no news/project mutation, project_bindings 0, authoritative_writer false and fail-closed core unchanged.

The visible status must say:
0 VERIFIED RELATIONSHIPS · 3 ABSTAINED GOVERNANCE SUMMARIES

The pack must distinguish PIN_VERIFIED from NOT_PROMOTION_ELIGIBLE and ABSTAIN. It may display only proven observation/publication timestamps. It must list next gates: role-specific corroboration, verified company number, canonical facility identity, then deterministic downstream join.

Use four small ui/cartridges modules, one compiler, one source manifest, one Atman verifier and one artifact-only candidate workflow. Keep facts authenticated inside the registry cache contract. No browser JSON payloads, startup requests, Python builder or new Parquet. No Pages permissions, git push or pointer changes.

10. COMPILE AND AUDIT CARTRIDGE SUCCESSOR

Deterministically double-build from clean roots and compare exact closure/hashes. Require no startup module/payload requests; one lazy import per explicit control; second open no extra request; all four payload requests remain zero. Preserve product sentinels/interactions and relationship assets byte-identically. Test desktop/390px, first paint ≤5 seconds and no more than 10% slower than same-run 202608282200 median. Bound initial decoded growth to 16 KiB and four lazy modules together to 20 KiB. Prove accessibility states and a negative missing-cartridge test in which the core remains ready with 100 rows and 11 columns. Upload artifact-only candidate and structured evidence; never promote it in this invocation.

If delays consume passes, preserve causal order. Do not skip promotion proof merely to reach cartridge work. If all ten checkpoints complete early, choose the highest-value remaining bounded dashboard cartridge, still artifact-only.

DATA, PRIVACY AND RECOVERY LAW

No Data Center Map scraping, Companies House bulk archives, Overpass dumps, raw monoliths, caches, logs, screenshots in source, pyc or __pycache__. Do not publish individual-person names. An LLM may propose candidates but cannot establish identity, ownership, operator, developer or project bindings. All relationships remain UNKNOWN/ABSTAIN until role-specific deterministic evidence exists. Parquet stays compact ZSTD with declared keys and pinned DuckDB 1.3.2 full typed readback.

CLASSIFICATION

Use exactly one: WAITING, FAILED/REJECTED, GREEN-UNVERIFIED, VERIFIED, or UNKNOWN/BLOCKED. GitHub “success” is not independent proof.

REPORT EXACTLY

VERIFIED REMOTE
FAILED/REJECTED
WAITING
UNKNOWN
STATE CHANGES
NEXT PERMITTED CHECKPOINT
ELAPSED TIME

Include exact SHAs, run/job/artifact IDs, hashes, counts and URLs. Name exactly one next causal checkpoint. Stop.

MERGE OVERRIDES

- This GitHub file and its timestamped manifest are authoritative over any truncated or stale automation baton.
- The authoritative ChatGPT task is both timer and executor. Its start-of-run timer audit, scope comparator, idleness law, ordered checkpoint queue and end-of-pass arming law are one indivisible controller.
- The GitHub contract-gate workflow validates and attests this source only. It does not schedule product work, deploy Pages, move pointers, write GlobalGrid or pretend it can re-arm a ChatGPT invocation that never started.
- Every future material change gets a new Europe/London timestamped prompt, verifier, manifest and contract-gate workflow. Never amend this version.
- A milestone is complete only from exact remote heads, manifests, hashes, run/job/artifact evidence, public bytes and browser proof required by that milestone. A filename or green workflow label alone is never completion.
- Preserve the repository deployment policy. The current legacy Pages chain is not valid for generation 202608282200; M01 must create the new hash-bound control plane without weakening the sole authorised Pages deployment boundary.
