# Recovery handoff: Grid Computation 10×

Read this file first after context compaction or in a new Claude/Codex session.
Do not reconstruct the work from chat memory.

## Mission / TrueSelf

The physical mission is electrification, not software output. GridAtlas,
Pipeline News, data-grid-gb and the federation exist to help competent people
understand and deliver real electrical infrastructure faster and more
truthfully. Presentation never outranks geometry, public records, declared
source data or reproducible measurement. Unknown is a valid state. Derived and
declared facts never share a label. Evidence is not proof. The Spider sees
widely and claims narrowly.

Authority source:
`data-federation-map-for-globalgrid2050-all-repos/trueself/20260812-globalgrid2050-federation-trueself-chatgpt.md`.
It is governance context subordinate to the binding repository requirements,
data contracts and human principal.

No file containing the literal doctrine name `QuantumSpawn` was found in the
two requested repositories on 2026-09-01. Do not invent one. The owner’s phrase
is implemented as a **derived operating pattern** only:

1. spawn independent candidate investigations in isolated branches/worktrees;
2. make each candidate carry its source, scope, proof and limitations;
3. reconcile candidates through the federation topology and shared board;
4. permit exactly one serialized promoter to update `main`/live pointers;
5. preserve losing, failed and unknown states as evidence rather than deleting
   them or painting them green.

## Current objective

Deliver ten substantive paired GridAtlas/Pipeline News generations, focused on
grid computation, then place exactly twenty verified links on the root
GlobalGrid2050 homepage: ten direct GridAtlas acceptance links and ten matching
Pipeline News journeys. Do not count cosmetic cuts, failed candidates or local
branches. Commit completed, audited work to `main`; never push a red candidate.

Full scope:
`docs/coordination/from-codex/202609012330-seven-hour-grid-computation-10x-scope.md`.

## State at handoff

Observed on 2026-09-01 near 23:55 Europe/London. Re-read Git before acting;
another agent may have advanced it.

- GridAtlas main: `dfc6722`, v9.71 generation `202609012243`, previous
  `202609012234`. v9.71 says a hop is a published circuit, not distance or
  capacity. It was on origin/main, but no subsequent `verified live` receipt was
  visible in the eight-commit snapshot. Treat live/CI acceptance as open until
  independently read back.
- v9.70: generation `202609012234`; had an attended verified-live receipt
  `d99cbbb` and was observed live before v9.71.
- Pipeline News main: `4077471`; main worktree has 12 pre-existing untracked
  files. Preserve them. Never clean/reset this checkout.
- data-grid-gb main: `1c9909d`, clean.
- globalgrid2050 main: `1f8ecfe9`, clean.
- spiders main: `de53761`, clean.
- federation ledger main: `b759a7d`, clean.
- CVAA ordinary checkout is intentionally dirty on
  `codex/202609012100-cvaa-mission`; do not use it for integration.
- Local Claude/Codex transcript harvester is installed as
  `GlobalGrid2050-Claude-Codex-Continuity`, every 15 minutes. Raw output is
  ignored at `coord24/docs/coordination/.local/transcripts-last-24h.jsonl`.

## Proven isolated candidates

1. Grid computation coordinator
   - branch `codex/202609011941-grid-computation`
   - commit `d5da5c8`
   - pure module `atlas/modules/202609011941-click-computation.js`
   - direct proof 13/13; local CI exit 0; composed 586; topology 47/47;
     source registry 24/24; map-click 13/13.
   - not merged/deployed at the snapshot.

2. Pipeline News MAP lineage audit
   - branch `codex/202609012206-pipelinenews-10x10`
   - commit `3724c9d`
   - 84,172 journeys across 11 immutable releases; 12/12 lineage and 11/11
     cross-repo contract checks; maximum URL 250 bytes.
   - not merged/deployed at the snapshot.

3. Historical-triangulation CVAA vaccine
   - branch `codex/202609012350-historical-triangulation`
   - commit `dfb8129`
   - locked CVAA registry; self-run immune.
   - requires a timestamped historical-triangulation gate and independent
     proof for versioned applications.
   - not merged at the snapshot.

4. Coordination and full scope
   - branch `codex/202609012115-board-continuity`
   - latest known commit `fa67044`
   - includes the full scope and requirement for twenty homepage links.

5. GlobalGrid2050 acceptance-index worktree
   - path `C:\Users\vikra\global-10x`
   - branch `codex/202609012340-global-10x-acceptance`
   - no completed acceptance-index commit at the snapshot.

## Mandatory promotion stack

Every release must pass these layers in order. A later layer cannot waive an
earlier one.

### 1. TrueSelf mission gate

Write one sentence answering: which physical electrification decision becomes
more truthful or executable? Reject work whose only answer is “another
version”, appearance, engagement, output volume or AI novelty. Check privacy
and commercial/confidential boundaries.

### 2. Spider doctrine gate

Follow the actual Spider sequence:

```text
study -> source-card -> scope -> scan -> distil -> validate -> render ->
visual test -> receipt -> human-accepted promotion
```

Enforce Spider DNA:

- the map is metadata, not the territory;
- unknown remains visible;
- declared and derived are separate;
- evidence is not proof;
- see widely, claim narrowly;
- scanners observe and derive but never promote derived findings into declared
  facts;
- topology is connectivity, not load flow or engineering certification;
- renderer consumes precomputed data and does not become a second calculator;
- loved/known-good surfaces remain byte-stable; evolution is additive.

Use `spider_maya`/federation scans to verify nodes, edges, endpoints,
dependencies, source ownership, schemas and live surfaces. Spider output is a
screening gate. Any finding affecting an engineering claim must be confirmed by
the owning data/product proof.

### 3. CVAA gate

Run the pinned CVAA registry against each changed repository with full history.
Do not accept shallow checkouts, skipped antibodies, changed vaccine hashes or
new baselines that widen a ratchet. Include the new historical-triangulation
vaccine after it is independently reviewed and merged.

### 4. Historical anti-hallucination triangulation

For every candidate select three independent Git baselines:

- far: a commit at least one month old, else the earliest available commit;
- middle: deterministic pseudo-random selection from the middle history, seeded
  by the candidate generation so CI is reproducible;
- near: last accepted safe predecessor, never the candidate itself.

The current repositories began 30 August 2026, so no one-month baseline exists
yet. State this and use earliest. Fail on zero comparable surfaces, missing
history, shallow checkout, candidate=self, duplicate selections or a fixture
authored only by the candidate. Compare behavioural contracts, not merely
strings. Record deliberate differences.

### 5. Local CI and data-owner proof

Run syntax, schema, adversarial fixtures, real owner-product proof, all-current
cartridge proofs, assembler/parts atomicity, cross-repo producer/consumer
contract, all-version scan, scope/state generation and clean-tree checks. Local
Node 24, Python 3.12 and PowerShell are available. WSL and Docker are not
available because virtualization is disabled. Do not block on them.

### 6. Git/Linux receipt

Push candidate branch first. Linux GitHub Actions are parity receipts, not the
primary compute engine. Require exact candidate SHA, full history where CVAA
needs it, pinned actions, least permissions and no competing contents:write
publisher.

### 7. Serialized promotion and live read-back

One promoter only. Fetch origin; require the candidate base equals current
origin/main; otherwise rebuild/rebase in a fresh isolated worktree and rerun
everything. Fast-forward/cherry-pick reviewed commits; never merge two modified
composition pointers. After push, fetch `current.json` and **every** referenced
artifact without cache, recompute hashes, check lineage and exact SHA/CI.

### 8. Mobile/browser acceptance

Exercise real Pipeline News MAP buttons, portrait and landscape. Minimum
archetypes: register-present; register-absent; declared 400 kV customer
substation; unbuilt/pink far end; DNO circuit/no line; BESS. Immediate public
record and identity must precede asynchronous engineering detail. No stale
card, stale distance, active failure or inert-looking wait.

### 9. Homepage acceptance matrix

Only after all ten pairs pass, compile twenty unique HTTPS links into root
`globalgrid2050/index.html`. Each pair states generation, project/archetype,
computation, expected visible result and far-end state. Compiler must require
10+10, unique URLs, monotonic accepted generations, HTTP success and deployed
byte read-back. Do not publish pending placeholders.

## Overnight runner warning

The existing runner previously:

- mutated ordinary `main` directly;
- lacked an exclusive lock;
- used broad checkout rollback;
- allowed `--dry` to leave a dirty tree;
- initially pushed two conflicting refspecs;
- committed live receipts without initially pushing them;
- coexisted with independent contents:write workflows.

Several repairs subsequently landed and v9.70/v9.71 reached origin, but do not
assume the architecture is now safe. Before unattended ten-cut execution,
prove lock contention, stale-lock refusal, injected failures before/after every
publication stage, no main mutation before promotion, exact base SHA,
origin-moved refusal, idempotent resume, parallel invocation (one winner/one
clean refusal), complete live artifact hashes and receipt publication.

## Exact first commands for a fresh session

Run read-only commands first:

```powershell
cd C:\Users\vikra\OneDrive\Documents\GitHub\gridatlas
git status --short --branch
git fetch origin --prune
git log -12 --oneline --decorate
Get-Content -Raw atlas\current.json
node tools\proofs\run-current.mjs
node tools\ci\202609012200-local-ci.mjs
```

Then:

```powershell
cd C:\Users\vikra\coord24
node tools\coordination\export-last-24h.mjs --hours 24 --output docs\coordination\.local\transcripts-last-24h.jsonl
node tools\coordination\continuity-proof.mjs
Get-Content -Raw docs\coordination\RECOVERY-202609012355-GRID-10X.md
```

Inspect, do not merge yet:

```powershell
git -C C:\Users\vikra\OneDrive\Documents\GitHub\gridatlas show --stat d5da5c8
git -C C:\Users\vikra\OneDrive\Documents\GitHub\pipelinenews show --stat 3724c9d
git -C C:\Users\vikra\OneDrive\Documents\GitHub\cvaa show --stat dfb8129
```

Next decision: independently verify whether v9.71 is live and green; review the
three isolated candidates; implement/prove the historical-triangulation
executable gate; then select one publication owner. Do not begin version 2 of
the ten until version 1 has a complete receipt.

## Context and evidence locations

- Shared board: `pipelinenews/docs/coordination/BOARD.md`
- This handoff: `coord24/docs/coordination/RECOVERY-202609012355-GRID-10X.md`
- Full scope: `coord24/docs/coordination/from-codex/202609012330-seven-hour-grid-computation-10x-scope.md`
- Raw local transcripts: `coord24/docs/coordination/.local/transcripts-last-24h.jsonl`
- Claude current transcript: `C:\Users\vikra\.claude\projects\C--Users-vikra\dfac5e26-89f2-47c9-acff-30f7eba5468f.jsonl`
- Codex rollouts: `C:\Users\vikra\.codex\sessions\2026\09\01\*.jsonl`
- Grid shift ledger: `gridatlas/tools/overnight/shift-log.json`

## Stop condition for this thread

Once this file and its board pointer are committed and pushed, start a fresh
thread/session. Paste only: “Read RECOVERY-202609012355-GRID-10X.md completely,
run its read-only recovery commands, then continue from the first unresolved
gate.” The repository evidence, not this conversation, is the continuation.
