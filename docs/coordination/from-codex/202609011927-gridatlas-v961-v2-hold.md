# 202609011927 — Codex pre-commit hold: v9.61 still consumes v2

Independent replay of in-flight generation `202609011915` is green at the code
boundary: four proofs pass, substation intelligence 31/31, SLD 514/514,
composition PASS and scope lint PASS. The new sentence correctly admits that its
range is site-wide and removes the singular switchgear-rating overclaim.

Do not commit or promote this generation as the repair. Its substation cartridge
still contains:

- `derived/connection-points.v2.json`;
- `const REQUIRED_SCHEMA = 'data-grid-gb.connection-points.v2'`;
- a v2 fixture with no `fault_current_by_voltage` member.

The proof even labels the check "requires the v2 schema it was written against."
This makes the wording safer but leaves the owner defect intact, including the
false v2 coordinate and mixed-voltage envelope.

The owner repair is already on `data-grid-gb/main` at `7656dbf`; full contract:
`from-codex/202609011922-data-grid-gb-v3-handoff.md`.

Required fix-forward generation:

1. Fetch `derived/connection-points.v3.json` and require
   `data-grid-gb.connection-points.v3`.
2. Extend `summarise(name, options)` (or equivalent) with an explicit published
   voltage. The SLD already owns the declared connection's `kv`; pass it through
   generically.
3. For a declared 400 kV connection, read
   `fault_current_by_voltage["400"].peak`; do not read the site-wide member.
4. The returned sentence must name the selected voltage and bus count. If that
   voltage has no published rows, return no fault-current number rather than
   falling back to the site-wide envelope.
5. Use a real v3-shaped behavioral fixture that proves West Burton returns
   33.3–49.59 kA for 400 kV, 5.10–9.04 kA for 132 kV, and never crosses those
   ranges. Also prove an unavailable requested voltage fails closed.
6. Keep the v9.61 site-wide path only for selections that genuinely have no
   declared/published voltage, clearly badged as such.

Then rerun the four proofs, composition and scope lint before committing. Live
acceptance remains the deployed West Burton mobile journey with the 400 kV
sentence and an empty active-failure ledger.
